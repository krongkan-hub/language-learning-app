# Backlog

Owned by `pm_agent`. Every open item (bug report, feature request, architecture follow-up) is tracked here. Detailed reproduction steps for resolved or historical bugs reside in [`bug_reports/`](bug_reports/).

## Status

The project is a CLI language-learning roleplay application using local LLMs via MLX on Apple Silicon for English and Japanese instruction.

The deterministic gate and all four LLM-graded suites were re-measured on **2026-09-05**; the surrounding narrative still dates from the **2026-08-15** sweep. Every deterministic gate is green and every suite is above its baseline.

The headline number to read here is still the coach's — **68.8%**, and the low absolute figure should not be read as a regression. The suite was expanded from 16 cases to 46 on 2026-08-15 specifically to find out how much Japanese it was missing, because 6 Japanese fixtures were not enough to answer that. The answer was: a lot. It scored 63.0% against that wider ruler, has since been netted up to 68.8% against a wider ruler again (72 cases). See OPEN-10 — this is still the largest known quality gap in the product.

### Deterministic gates — [`scripts/check_all.sh`](scripts/check_all.sh), all passing

| Check | Result |
| :--- | :--- |
| `pytest` | 370 passed |
| `pyflakes` (`app/ scripts/ main.py`) | clean |
| [`check_task_depth.py`](scripts/check_task_depth.py) `1-80 --expect-total=5520` | pass |
| [`check_scenario_parity.py`](scripts/check_scenario_parity.py) `1-80` | pass |
| [`check_content_coherence.py`](scripts/check_content_coherence.py) | pass (6/6 sub-checks) |
| [`check_catalog_roundtrip.py`](scripts/check_catalog_roundtrip.py) | 80 scenarios, 5,520 tasks, sha256 `155d362b285d50fc` |
| coverage floor (≥80%) | 86% |

The catalog comprises 80 scenarios × 69 tasks = 5,520 tasks stored in [`app/scenarios/data/scenario_01.json`](app/scenarios/data/scenario_01.json) … `scenario_80.json` and loaded via [`app/scenarios/builtins.py`](app/scenarios/builtins.py).

### LLM-graded suites — not run by `check_all.sh`; gated separately by [`scripts/check_evals.sh`](scripts/check_evals.sh)

| Suite | Result | Note |
| :--- | :--- | :--- |
| [`eval_actor.py`](scripts/eval_actor.py) | **90.0% (90/100)** on 2026-09-05, baseline 84.0 | 20 cases × 5, 10 EN / 10 JA, all 6 moods. Read 96–99% (3 runs) on 2026-08-15, then drifted, which is why the floor was lowered to 84.0 in 27f6da1. The 2026-09-05 reading is the first since the shared-rule change — plausibly the revived markup rule (OPEN-16) doing work, but **one run is not a trend and the baseline was deliberately left at 84.0** until a second measurement agrees. Remaining failures are all one class: the vocab block — see OPEN-12 for what this suite does *not* catch |
| [`eval_moods.py`](scripts/eval_moods.py) | 100% (96/96), 3 runs | 6 moods × 2 languages × 2 conversations × 4 turns, 12 sampled scenarios. Ruler 5.3× longer and now bilingual; score did not move. **Reads the repair pipeline, not the actor — OPEN-14** |
| [`eval_coach.py`](scripts/eval_coach.py) | **68.8%** on 2026-09-05 (68.2% on 2026-08-15), baseline 65.0 | 72 cases × 5, headline excludes 6 prompt-example cases that cannot fail (70.8% including them). Ruler 16 → 46 → 52 → 72; the jump from 59.6% is the transitivity and counter nets — OPEN-10, OPEN-11 |
| [`eval_judge.py`](scripts/eval_judge.py) | **93.3% (140/150)** | 30 cases × 5; 1/17 false negatives, 1/13 false positives. Four cases added for the English-target-in-Japanese path — OPEN-01, OPEN-18 |
| [`playtest_sample.py`](scripts/playtest_sample.py) | 99.5% (199/200, seed 42) | last full run; its 1 failure (Sc28 task 11) is fixed and re-playtested 3/3 — OPEN-09 |

**Read the 99.5% narrowly.** The playtest measures *task completion* — whether a simulated learner can get the NPC to the goal, graded by the judge. It says nothing about whether the learner's language was corrected, so it is blind to the coach by construction. "99.5% of tasks are winnable" and "the coach misses ~3/4 of Japanese errors" (OPEN-10) are both true at the same time, and only the first one looks like a headline. Nothing in the suite table above measures whether a learner actually improves.

No fixture was deleted, reworded, or relabelled to reach these numbers. [`eval/eval_baselines.json`](eval/eval_baselines.json) floors each suite below its measured score, because every suite samples only a handful of iterations and a single flaky run is normal — the actor read 96 / 98 / 99 per 100 across three consecutive runs with no code change. A real regression moves more than one run.

The actor expansion (4 → 20 cases) is the mirror image of the coach one: the score went **up**, so the old 4 cases were thin rather than wrong, and no new failure class appeared. The value was in what it surfaced outside the score — OPEN-12.

## Open Items

| ID | Title | File / Context | Status & Note |
| :--- | :--- | :--- | :--- |
| OPEN-01 | Judge false negatives and one false positive | [`app/judge.py`](app/judge.py), [`eval/judge_cases.json`](eval/judge_cases.json) | **Partly fixed; 1 FN and 1 FP remain.** Was 84.6% (110/130) with 3/15 false negatives (cases 22, 25, 26) and 1/11 false positive (case 5); now **92.3% (120/130), 1/15 FN, 1/11 FP**. Cases 25 and 26 were the context bug — the judge graded the learner against the NPC's last question — and both are fixed by re-checking a NO against the learner's sentence alone and letting only a context-free YES overturn it. The strict context-aware pass is unchanged, so this cannot repeat the two reverted restructures (each took FPs from 1 to 5). Multi-clause AND goals are excluded from the second opinion: unguarded it made case 15 a false positive, which is exactly how those restructures failed. Case 22 (an OR goal in Japanese, no context at all) is untouched by this and remains a 7B limit. Case 5 stays the accepted English trade. Cost: one extra 64-token call on the NO path only. |
| OPEN-07 | Coach misses Japanese particle error, reports "Perfectly natural!" | [`app/coach.py`](app/coach.py), [`eval/coach_cases.json`](eval/coach_cases.json) case 14 | **Fixed.** Case 14 went 0/5 → 5/5 and the suite 93.8% → 100% (80/80), with case 15 holding 5/5 so nothing was traded for it. The cause was not prompt bias: asked outright, with no coach prompt and no leniency instruction, the model calls 「友達を会いました」 *完全に正しい*. Adding the rule to `COACH_SYS`, then a near-verbatim worked example, both left it at 0/5, so the prompt lever is exhausted at 7B. `apply_particle_net` instead catches を on the target of a に/と-taking verb (会う, 乗る) in code — the same deterministic-augmentation route `101439f` took for the judge — and only ever overturns a "Perfectly natural!"; a real model correction always wins. Covered by pure-logic tests including causatives (`友達を医者に会わせた`) and `乗せる`, where を is correct. **Follow-up fix, same day:** the first version of the 乗 rule matched a stem class and so fired on every compound built on 乗る, "correcting" `電車を乗り換える`, `駅を乗り過ごす`, `飛行機を乗っ取る` and `困難を乗り越える`, all of which take を. No coach fixture contained 乗, so the suite read 100% with the bug live in a catalog full of train scenarios. The 乗 branch now lists whole inflections of plain 乗る, which also survives kana spelling (`電車を乗りかえます`) where a "no kanji after the stem" rule would not. `電車を乗ります` is still caught. Guarded by `test_particle_net_stays_quiet_on_wo_taking_compound_verbs`. Lesson for the next net: a net is only as safe as its must-stay-quiet fixtures. |
| OPEN-08 | `check_all.sh` omits every LLM suite | [`scripts/check_evals.sh`](scripts/check_evals.sh), [`Makefile`](Makefile) | **Fixed.** [`scripts/check_evals.sh`](scripts/check_evals.sh) (`make check-evals`) runs the coach, judge, actor and moods suites and fails on a drop below [`eval/eval_baselines.json`](eval/eval_baselines.json); the judge additionally gates on false-negative and false-positive counts, since a steady score can hide FNs growing. Deliberately **not** added to `check_all.sh` or CI — it needs MLX and takes minutes, and the deterministic gate must stay fast. Both the pass and the regression path were verified end to end (exit 0 / exit 1). Unrelated hazard, still true: `playtest_sample.py` resumes from its `--out` file by design, so re-running against an existing results file replays old numbers without touching the model. Use a new `--out` path when verifying. |
| OPEN-09 | Playtest failure: Sc28 task 11 | [`app/scenarios/data/scenario_28.json`](app/scenarios/data/scenario_28.json) | **Fixed.** The task asked the learner to "confirm" a case reference number the scenario never gave them, so learner and NPC each waited for the other and the turn budget ran out. The `scene_hint` now puts reference number LP-2291 on the counter for the NPC to state, and the goal/`done_when` ask the learner to read it back rather than produce it. Re-playtested 3/3 (was 0/1). Task count unchanged at 5,520; catalog sha256 is now `155d362b285d50fc`. **Follow-up fixed 2026-08-19:** the judge also accepted a number the learner invented, so 2 of the 3 passing runs never used LP-2291. Reproduced directly — told to read back LP-2291, the LLM judge passed `XY-9999` and even `I have written down the reference number`; it grades the shape of the answer, not its content. `judge_identifier_readback` now requires an exact match, but only when the goal is about repeating an identifier AND the NPC actually supplied one; otherwise it returns `None` and defers to the LLM, pinned by must-stay-quiet tests (a bare quantity like "two tickets" is not an identifier). Judge suite unchanged at 92.3%, 1 FN, 1 FP. |
| OPEN-10 | Coach misses ~3/4 of Japanese learner errors | [`app/coach.py`](app/coach.py), [`eval/coach_cases.json`](eval/coach_cases.json) | **Open — largest known quality gap.** Measured 2026-08-15 by expanding the coach suite from 16 cases to 46 (Japanese 6 → 36, including 9 new `clean` cases). Score fell 100% (80/80) → **63.0% (145/230)**; English stays 100%, Japanese is 53%. **17 of 23 new error cases fail, 16 of them at 0/5** — confident, not flaky. Blind classes: particle choice 1/7 (を/に, に/で, で/に, に/と, は/が), transitive/intransitive **0/3** (`電気をつきました`, `会議が始めました`, `窓が閉めました`), register 1/3 (`先生、俺は宿題を忘れた`), counters 1/3, word order 1/3. **The failure is pure under-detection, not over-correction: all 12 clean cases pass 5/5.** The coach answers "Perfectly natural!" to real mistakes. Prompting is exhausted and has gone *negative* — `COACH_SYS` carries a たくさん→とても worked example and the model now applies that swap in both directions blind to context, "correcting" とても→たくさん; it is retrieving the example, not parsing the sentence. Next steps, in order: transitive/intransitive pairs and counters are closed enumerable sets and are net-able (each needs must-stay-quiet fixtures first, per OPEN-07); particle choice, register and word order need meaning and context, so they need a larger or Japanese-tuned model, not a net. **Do not chase this number back to 90% with nets — the figure to defend is clean 12/12, since over-correction harms a learner more than a miss.** **Tried and reverted:** deleting the たくさん→とても worked example from `COACH_SYS`, on the theory that the model was retrieving it rather than parsing. It is not that simple — the two cases blamed on it (37, 39) did not move at all, 0/5 before and after, while the change cost 12 iterations overall (63.0% → 57.8%) by breaking case 8 (5/5 → 0/5, its own sentence), case 38 (5/5 → 0/5) and case 1 (5/5 → 2/5). Prompt edits here move unrelated cases and are not a cheap lever; measure the whole suite before and after, never just the cases you are aiming at. **Mitigated meanwhile:** the Japanese clean verdict now reads 「特に直すところは見つかりませんでした。」 instead of "Perfectly natural!", so a missed error is reported as the checker finding nothing rather than as a verdict that the sentence is right — at 53% detection the confident version actively confirms the learner's mistake. Localization happens once at the end of `coach_feedback`, after the particle net, so `COACH_SYS` is untouched; score and clean 12/12 both unchanged. | **Net-able half CLOSED 2026-09-02.** `apply_transitivity_net` and `apply_counter_net` take the coach from **59.6% → 68.2%**, with the 21 clean cases holding at 5/5 — zero over-correction. Transitive/intransitive 0/3 → 3/3 (`電気をつきました`, `会議が始めました`, `窓が閉めました`); counters 1/3 → 3/3 (`水を三枚`, `切符を二本`, `りんごを二本`). Both are deterministic, so unlike a prompt-driven gain this should not decay. Three design decisions carried the result, each found by a probe rather than by reasoning: (a) the transitivity net must quote **the learner's own inflection** back — its first version emitted the citation form 「をつけます」 where the fixture and the learner both wanted 「をつけました」, which scored 0/5 while passing 20/20 in isolated probes; (b) the verb must sit **immediately** at the particle, since searching ahead matched a later clause's verb and "corrected" 4/4 correct multi-clause sentences; (c) the counter net is a **deny-list**, because つ/個 are near-universal and 水 legitimately takes both 本 and 杯, so an allow-list would over-fire. **What remains is the model, not code:** particle choice (1/7), register, and word order need meaning and context. Three separate lines of evidence today put that at a 7B capability limit — the model calls 友達を会いました 完全に正しい when asked directly, prompt rules and worked examples both left it at 0/5, and a reroute rule measured perfectly inert. That is a decision about the coach model, not another net.
| OPEN-11 | 6 coach fixtures are verbatim copies of `COACH_SYS` examples | [`eval/coach_cases.json`](eval/coach_cases.json), [`app/coach.py`](app/coach.py) | **FIXED 2026-08-19, and the effect is now measured rather than inferred.** The six were kept and tagged `prompt_example`, six new cases testing the same rules with novel sentences were added (suite 46 → 52), and `eval_coach.py` now reports the honest headline **59.6% (137/230)** with the full **64.2% (167/260)** printed beneath for continuity. **The contaminated six scored 30/30; their novel equivalents scored 22/30.** Sharpest pair, same rule: `two bottle` (quoted in the prompt) 5/5 versus `four ticket` (novel) 2/5. Two other pairs matched at 5/5, so this is not blanket memorization — the model does know "to + base verb" and the participle rule; plural-after-a-number was being carried by the prompt. [`scripts/check_fixture_contamination.py`](scripts/check_fixture_contamination.py) now runs in `check_all.sh` and fails if any untagged fixture is quoted in its own prompt, verified in both directions. Original report follows. Cases 5, 6, 7, 8, 11 and 12 appear word for word inside the prompt's EXAMPLES block (`I want to finding a book.`, `コーヒーを一つ欲しいだ、ブラックで。`, `ブラックコーヒーをください。`, `わたし、猫が好きだ、たくさん。`, `Is it prohibit here?`, `Can I get two bottle of water, please?`). The model is handed the answer, so these cases cannot fail and they inflate the score — six of them, against a surviving English set of ten. Discovered when deleting one worked example dropped its twin fixture from 5/5 to 0/5. Excluding all six, the honest score is **115/200 = 57.5%**, not 63.0%. Fix is to rewrite the six as fresh sentences probing the same error classes; that means editing existing fixtures, which is normally forbidden, so it wants a deliberate decision rather than a drive-by. |
| OPEN-14 | The actor suites measure the repair pipeline, not the actor | [`app/llm.py`](app/llm.py) (`call_actor`), [`scripts/eval_actor.py`](scripts/eval_actor.py), [`scripts/eval_moods.py`](scripts/eval_moods.py) | **Open — a ceiling effect, not a defect.** `call_actor` retries up to 3×, then salvages, then falls back, so `validate()` at the eval layer only ever sees what survived all of that. Raw first-attempt generations are now measured by [`scripts/eval_rawactor.py`](scripts/eval_rawactor.py): **68.1% (98/144) over three readings** (70.8 / 68.8 / 64.6), English 79.2% mean, Japanese 57.0% mean. The **16.7–33.3%** previously recorded here is superseded and was measured wrong — that probe scored greetings against `max_sentences=3` when greetings get 4, and passed no `language`, so it counted compliant greetings as failures and scored every Japanese script leak as a pass. The repair pipeline is worth roughly 20 points, not 60. Every point between those and the suites' 95–100% is the repair loop. Dominant first-attempt failure is `Too many sentences (4/5/6)`; Japanese adds `Wrong script` and `Closed yes/no question`. Two consequences. First, the moods suite cannot really move — `validate()` is a format checker downstream of a repair loop whose fallback line passes in every language, so it is an honest measure of *what the learner receives* and a near-useless measure of *how good the actor is*; the headroom lives in the first-attempt number, which nothing currently tracks. Second, this is why the OPEN-12 script rule and the Japanese closed-question rule both show zero effect in the suites while demonstrably firing on raw output — they are working, upstream, where no suite looks. A first-attempt compliance metric now exists. It is deliberately **not gated**: at n=48 the binomial standard error is 6.7 points, the Japanese greeting cell swung 75/75/25 across three readings of identical code, and a floor loose enough to survive that would not catch a real regression. Its value is the **reason distribution** — 27x closed yes/no question, 11x too many sentences, 8x wrong script over 144 samples — which says which rule the actor trips and which no repaired score can show. |
| OPEN-19 | Japanese scenario descriptions written in **Chinese** | [`app/scenarios/data/`](app/scenarios/data/) (`place_translations`, `name_translations`), shown via [`app/i18n.py`](app/i18n.py) `scenario_place` | **FIXED 2026-08-30.** 15 strings rewritten in authored Japanese — 13 that were Chinese (`闹市区的咖啡馆` → `近所のにぎやかなカフェ`, `附近的药店` → `近所の薬局`, `计算机和手机修理店` → `パソコンと携帯電話の修理店`), plus `科学的なカラオケルーム` ("scientific karaoke room") which was valid Japanese with the wrong meaning, from "A private room karaoke venue". Verified 0 entries carrying simplified-Chinese characters remain. **The original count of 17 was wrong, from my own over-firing detector** — I flagged on 的, which is a legitimate Japanese adjectival suffix, so `近代的なフィットネスセンターの受付`, `清潔で現代的な歯科クリニック`, `伝統的な靴修理工場` and `現代的なコワーキングスペース受付` were all correct Japanese wrongly accused. Same bug class as everything else in this audit, this time in the detector rather than the code. **Two things this surfaced, both still open:** (a) the catalog sha256 did **not** change — `155d362b285d50fc` before and after — so `check_catalog_roundtrip` covers tasks only and these 160 learner-facing translation strings sit outside every integrity check; (b) a number of surviving translations are Japanese but awkward or over-literal rather than wrong: `視光学眼鏡` (optician), `緊急病院初期受付` (ER triage), `交通警察道路脇停止` (roadside stop), `警察署失物拾得` (lost property — 遺失物 is the correct term). Those are a content-quality pass, not a script bug. Original report follows. **Learner-facing catalog content, found incidentally.** `place_translations['Japanese']` holds Chinese for 17 scenarios and `name_translations` for 1: Coffee Shop → `闹市区的咖啡馆`, Pharmacy → `附近的药店`, Hotel Check-in → `4星级宾馆的前台`, Car Rental → `空港的租车柜台`. Detected by the Chinese possessive 的 and by `find_wrong_script`. These are displayed in the scenario chooser ([`app/cli.py`](app/cli.py) lines ~282, ~304), so a Japanese learner reads Chinese before a session even starts. Others are not Chinese but garbled Japanese (`ハーレムホームセンター`, `キュリキュラリーヨークの教室`). This is authored content, not a code path — it needs 18 corrected translations, and machine translation is what produced the problem. Note the catalog sha256 will move, so `check_catalog_roundtrip` and `eval/catalog_snapshot.json` both need regenerating. **Related:** this is why comparing a vocab word against the translated identity was rejected as the fix for OPEN-17 — the identity strings are themselves unreliable. |
| OPEN-17 | `_is_trivial_vocab` cannot fire on Japanese | [`app/cli.py`](app/cli.py) (`_words`, `_is_trivial_vocab`, `_is_venue_noun`) | **FIXED 2026-08-28 (F7). F6 declined with evidence.** Measured first: 108 Japanese and 75 English vocab cards, hand-labelled, paired on identical scenario+task+mood. **Japanese junk rate 24.7% (23/93) against English 1.9% (1/52)** — 13× worse in practice, not merely unguarded in theory, and of the 23 junk tips written in Japanese script the shipped guards caught **zero** (the 7 that were caught all happened to contain a Latin word). All 23 reached `vocab_log` and 21 came back as quizzable rows. `_is_venue_noun` now matches the word's own shape — suffixes that turn a noun into a place-of-business or job title (店 局 院 所 場 館 受付 センター 屋さん 手 官, plus 室 at length ≥3), requiring the word to outgrow the suffix it ends with. Every suffix was measured to catch a real junk card with zero false positives; plausible ones with no measured catch were left out. Both guards came from data: 室 needs the length floor or it eats `個室`, and the outgrows-suffix rule came from the actor teaching bare `受付` as a real tip. **Junk 24.7% → 9.1%, legitimate cards kept unchanged at 70/78 — zero new over-fires**; `vocab_log` junk 23 → 7. **F6 (`_is_name`) declined:** zero proper nouns in 108 Japanese cards — the Japanese actor never invented a business name — and the proposed explanation-marker signal (`…の名前です`/`店名`) appears in zero Japanese explanations, while in English it fires 2 true against 4 false. Building it would add over-firing for no measured benefit. Original report follows. **Fifth instance of the ASCII-assumption class.** `_words()` tokenizes with `re.findall(r"[a-z']+", ...)`, so it returns an empty set for any Japanese string — verified, `_words('薬局 は 店') == set()`. `_is_trivial_vocab` is built entirely on it, so the guard that stops the actor "teaching" the learner the venue's own name never fires in a Japanese session. **A script-aware tokenizer alone would not fix it:** scenario `name`/`place`/`role` are stored in English (`An airport check-in counter`) while the vocab word is Japanese, so there is nothing to compare even after tokenizing correctly — the identity strings would need translating too. Wants its own measurement of how often a trivial Japanese card actually reaches the learner before deciding how far to go. |
| OPEN-18 | The 401 vocab-gated tasks are graded by LLM in Japanese, not deterministically | [`app/judge.py`](app/judge.py) (`judge_deterministic`), [`app/scenarios/data/`](app/scenarios/data/) | **Open — the deferred proper fix for the F1 unwinnability bug, which is itself fixed.** `done_when` stores its vocab target in English for all 401 gated tasks (`Learner used the word 'sommelier'.`), verified: 401/401 targets are ASCII, 0 contain a Japanese character. The Japanese branch substring-tested that English target against Japanese learner text, so the verdict was a constant `False` and `evaluate_task` — which returns on the first non-`None` result — never reached `judge_llm`. The learner read the translated goal 「ソムリエを使用する」, typed ソムリエ, and was told they had not used 'sommelier', for all 4 attempts. **Shipped fix 2026-08-23:** when the session is Japanese and the extracted target contains no Japanese character, `judge_deterministic` returns `None` and defers to the LLM judge, which does credit the concept — the three real catalog tasks probed end to end against the model (`decaf`/カフェインレス, `sommelier`/ソムリエ, `prescription`/処方箋) went `(False, …)` → `(True, None)`, and off-topic Japanese controls on the same three still return `False`. The fallthrough is conditional on the **target's script, not the session's**, so a task whose target genuinely is Japanese (`Learner used the word '予約'.`) is still graded in code and still passes; judge suite unchanged at 92.3%, 1 FN, 1 FP. **What is deferred:** an authored Japanese target per task (e.g. a `vocab_target_ja` field on `Task`) so these 401 are graded exactly again rather than argued about with a 7B model. **Honest cost:** 401 authored translations — machine translation is not good enough here, since the whole point is that the string is the answer key; a schema change to `Task` plus the loader/serializer; the catalog sha256 moves off `155d362b285d50fc`, so [`check_catalog_roundtrip.py`](scripts/check_catalog_roundtrip.py) and [`eval/catalog_snapshot.json`](eval/catalog_snapshot.json) both need regenerating; and `check_scenario_parity` / `check_content_coherence` need re-running against the new shape. **Also worth measuring first:** the LLM judge is now the grader for ~1.3 tasks per Japanese session and no fixture covers that path — [`eval/judge_cases.json`](eval/judge_cases.json) case 23 is the *deterministic* Japanese target, and there is no case for an English target judged in Japanese. Adding one or two is much cheaper than 401 translations and would say whether the deterministic route is worth buying back at all. **Sixth instance of the ASCII-assumption class** after `is_closed_question`, the judge's identifier regex, the coach's Japanese fixtures, `has_question` (OPEN-15) and `_words` (OPEN-17). | **RESOLVED AS NOT REQUIRED 2026-09-04, on evidence rather than assumption.** Four judge fixtures were added to test the path `6cc6db2` created, because two fixtures cost far less than 401 authored translations and answer the same question. The LLM judge handles it: `デカフェ` (katakana borrowing) 5/5 credited, `カフェインレスのコーヒー` (native equivalent, no borrowing) 5/5 credited, and both negative controls correctly refused — off-topic `お手洗いはどちらですか` 5/5 and `薬をください` for 'prescription' 5/5. A grader that passed everything would have been worse than the constant `False` it replaced, so the negatives were the point. The one initial failure was not a judging failure but the walk-back bug in a new phrasing — verdict NO above a reason reading 「decaf」を用いて注文をしました — now fixed and 5/5. **So the 401 authored targets buy determinism and one saved LLM call per vocab task, not correctness.** The fixtures are permanent, and `check_evals.sh` gates the judge on false-positive count, so this path cannot silently rot.
| OPEN-15 | Japanese NPC turns end with an **English** question | [`app/llm.py`](app/llm.py) (`stream_actor`, `salvage_actor_output`, `_get_salvage_question`) | **FIXED 2026-08-22.** On a paired 48-turn sample (identical model output replayed through both arms): turns ending in an English salvage line **48/48 → 0/48**, and the learner's genuine Japanese question surviving **15/35 → 35/35**. `is_question` now dispatches on script, reusing `_JA_QUESTION_END`, and is deliberately *not* the complement of `is_closed_question` — a closed Japanese question is still dropped and still earns a salvage line, which is pinned by a test. `SALVAGE_QUESTIONS_JA` and `FALLBACK_ACTOR_LINE_JA` supply Japanese equivalents; English is byte-identical when no language is passed. Actor 97.0%, moods 100.0%, both above floor. **Known side effect:** freeing the reserved slot lets more model sentences through (mean 1.96 → 2.54 per turn), and in 1 of 48 turns the newly-surviving sentence was prompt leakage (`ご予約はお取り meticulous and precise service…`). The buggy slot rule had been incidentally suppressing that; no rule catches prompt leakage, and none was added. Original report follows. **The most learner-visible bug known at the time, and worse than the Chinese leak it was found next to.** `has_question` is computed as `'?' in s`, an ASCII character Japanese sentences never contain, so a Japanese question is never recognised as one. Two things then happen: the reserved-last-slot rule drops the learner's genuine question, and an English salvage line is appended in its place. Reproduced directly — input `こんにちは、本屋へようこそ。/ 本日は新刊が入荷しました。/ 何をお探しですか。` reaches the learner as the first two sentences plus `What would you like to sort out first?`. Measured at **94 of 144** sampled Japanese streamed turns. `salvage_actor_output` (line ~425) has the identical bug, so the `call_actor` path is affected too. Fix is to detect a question by script — full-width `？` and the か-final pattern already implemented for `is_closed_question` — and to make `SALVAGE_QUESTIONS` language-aware, since an English fallback question has no business in a Japanese session regardless. **Fourth instance of the ASCII-assumption class** after `is_closed_question`, the judge's identifier regex, and the coach's 6 Japanese fixtures. |
| OPEN-16 | The streaming markup rule is dead code | [`app/llm.py`](app/llm.py) (`stream_actor`) | **Declared FIXED 2026-08-25 — that fix did not work. Actually fixed 2026-09-05.** The 2026-08-25 entry below claimed the class mirrored `validate`'s exactly; it did not. The replacement was written raw, `r'[*\\[\\]<>]'`, which parses as the class {*, \, [} followed by the literal text `<>]`, so it matched only that exact sequence and still never a lone bracket. The rule stayed dead for eleven days under a FIXED label, which is the worse failure here: a measured 0.00% firing rate was read as "the shape does not occur" when it also reads as "the rule cannot fire". Both copies of the per-sentence rules are now one function, `sentence_rejection_reason`, so `validate` and `process_spoken` cannot disagree again, and a test drives sentences through the streamed path and asserts the emitted set equals the set that function accepts (0df1d3f, see OPEN-13). Original 2026-08-25 entry follows, and its class-mirroring claim should be read as the intent, not the result. `We have [espresso] today.` is dropped mid-stream instead of shown, and the assembled turn no longer fails its own validator. Measured firing rate on 623 captured actor turns: **0.00%** — the shape occurs only on the learner side, so this restores an invariant rather than changing observed behaviour. **Related change deliberately NOT made (audit finding F3):** `sanitize` remains ASCII-only. Across 152 Japanese turns — including 24 generated with the anti-narration clause removed to provoke narration — the model produced **0 full-width stage directions, 0 ＊ and 0 【】**, while every full-width bracket found was content worth keeping (18 glosses: `収入比（債務対収入比率）`, `烤鸭（かがも）`). Widening to （）would delete glosses to catch narration that does not occur; the ASCII rule is already observed destroying a Japanese reading gloss `パーム (パーム)`. Widening to ＊ was implemented, measured inert on 775 turns, and reverted — a ＊ span straddling the `word:`/`explanation:` labels destroys the entire vocab card, which is the dominant actor failure class, so it was zero benefit against a real latent cost. Revisit only with evidence that full-width narration actually occurs. Original report follows. **Open, small.** `process_spoken` writes the residual-markup check as a **raw** string, `r'[*\[\]<>]'`, so the character class parses as `{*, \, [}` followed by the literal `<>]` and it matches only that exact sequence — never a lone `[`, `*` or `<`. `validate()` line 273 has the same text as a non-raw string and is correct. Confirmed by driving the real `stream_actor`: `We have [espresso] today.` is emitted to the learner, and `validate()` on the assembled turn then returns `Contains residual markup characters`. So the streamed path ships markup that the checked path would reject. One-character fix, but it changes streaming behaviour, so it wants its own measurement rather than a drive-by. |
| OPEN-13 | The streamed conversational turn skipped the script check | [`app/cli.py`](app/cli.py) line 662, [`app/llm.py`](app/llm.py) (`stream_actor`) | **FIXED 2026-08-21 for the script rule; the structural cause closed 2026-09-05.** The 2026-08-21 entry threaded `language` through and re-implemented the rule, but left the design that caused the gap intact: `process_spoken` kept its own copy of the per-sentence rules, so every future rule had to be written twice and a rule added to `validate` still would not bind the path the learner actually reads. OPEN-16 is what that costs — a rule was added to both, one copy was broken, and nothing noticed for eleven days. Both copies now dispatch to one function, `sentence_rejection_reason(sentence, language)` in [`app/llm.py`](app/llm.py), pinned by `test_streamed_and_assembled_paths_agree_sentence_by_sentence` (0df1d3f). Sentence-count ordering was preserved deliberately: it still runs before the per-sentence rules so "Too many sentences" keeps its attribution in the retry note and the eval logs. Original 2026-08-21 entry follows. **Mostly fixed 2026-08-21 — the original entry overstated the gap.** `stream_actor` does not call `validate()`, but it has always re-implemented four of its rules inline in `process_spoken`: emoji, residual markup, `is_closed_question` (so the Japanese branch from 95feb3b already reached the shipped path) and the `max_sentences` cap. The one rule genuinely missing was OPEN-12's `find_wrong_script`: `stream_actor` took no `language` argument at all, so the 30% -> 0% leak fix bound only the greeting and skip-reply paths, which go through `call_actor`. `language` is now threaded from [`app/cli.py`](app/cli.py) line 662 through `produce_actor_turn` into `stream_actor`, checked per sentence **before the callback fires** — a sentence the learner has already read cannot be retracted, so this cannot be a post-hoc `validate()` — applied to the vocab block at assembly exactly as `call_actor` already did on its fallback path, and passed to both `call_actor` fallbacks. Measured on the streamed path itself, mid-conversation (4-turn Japanese conversations, 27 distinct scenarios, all 6 moods, seeds 42 and 7): **9/144 turns leaked before (6.2%) — 6/144 reached the learner's screen and 4 of the 10 vocab cards produced were Chinese — vs 0/120 after.** No turn fell back to the canned line in either arm; sampling is stochastic, so the arms are matched on scenario and mood, not on identical generations. **Still open:** (a) `stream_actor` duplicates the rules rather than calling `validate()`, so the next rule added to `validate()` will silently miss the conversational path again; (b) the residual-markup rule in `process_spoken` is dead — written `r'[*\\[\\]<>]'` as a raw string it matches the literal sequence `<>]` preceded by `*`/`\`/`[`, not any one of those characters, so `We have [espresso] today.` streams to the learner and the assembled turn then fails `validate()`; (c) no committed suite drives `stream_actor` — [`scripts/eval_actor.py`](scripts/eval_actor.py) and [`scripts/eval_moods.py`](scripts/eval_moods.py) both call `call_actor`, so the streamed path is still measured only ad hoc; (d) 94/144 sampled Japanese turns ended with an **English** salvage question, because `has_question` looks for an ASCII `?` that Japanese never contains — a separate learner-facing defect shared with `salvage_actor_output`. |
| OPEN-12 | Japanese actor turns leak simplified Chinese, and nothing checks output language | [`app/llm.py`](app/llm.py) (`validate`, `is_closed_question`), [`scripts/eval_actor.py`](scripts/eval_actor.py) | **FIXED 2026-08-17.** Leak rate on a 30-sample probe went **30.0% → 13.3% (retry loop enforcing) → 0.0%** (once the fallback path stopped re-attaching a bad card). `find_wrong_script` is a denylist of simplified-only forms, not a Han check — Japanese uses kanji throughout — with 没 deliberately excluded as an ordinary Japanese kanji (沈没), verified quiet against 105 genuine Japanese strings and pinned by must-stay-quiet tests. It runs on the full text, before `validate()` strips the vocab block, since that is where most leakage sat. The `is_closed_question` half is also fixed: it now dispatches on script, so `列車のチケットが必要ですか。` is caught while `何をお探しですか。` and `コーヒーはいかがですか？` stay open (いかが is treated as open for parity with English "How about a coffee?"). Actor suite unaffected at 95–97%. **Hole closed 2026-08-23:** `_JA_INTERROGATIVES` held bare substrings, so the indefinite pronouns 何か / どこか / 誰か / いつか rescued closed questions built on them — 「何かお手伝いできることがありますか？」, the single most common Japanese service greeting, was classified open. Measured on 157 captured sentences: the old rule flagged **0 of 50** question-shaped ones; the new rule flags 6, all verified correct by hand. Three guards were required and each was found by running against real output, not by reasoning: か opens the ablative から (「まず何からいたしましょうか。」 is `SALVAGE_QUESTIONS_JA[0]` — a naive strip made the app's own repair line fail its own validator); 何か、 at a clause boundary is a genuine embedded interrogative; and medial または/それとも/あるいは mark alternative questions, mirroring the English branch's `' or '` exemption including its position sensitivity. Casual plain-form ？-questions are also caught now, but only with a kana predicate (`[ぁ-ん]？$`) — bare-noun 「領収書？」 is deliberately *not* flagged, because it is structurally identical to the open 「お名前？」 and nothing distinguishes them; over-firing drops a real question and replaces it with a canned line, so neither is flagged. English verified identical over 18,696 sentences. Raises Japanese first-attempt rejection 54% → 77%, which is the rule working; moods fallbacks moved 0/96 → 1/96 as the expected cost. **Follow-up worth doing:** the actor opens with 何かお手伝いできることがありますか constantly, so the learner now often gets a salvage line instead — fixing the cause needs a Japanese actor-prompt change and its own eval run. | **Caveat: both rules only bind where `validate()` runs — see OPEN-13.** **Structural limit found 2026-08-22:** a character denylist cannot catch Chinese built from characters that are individually valid Japanese kanji — `或是者染めですか` (a 或者 construction) passes, because 或, 是 and 者 are all legitimate Japanese. Catching that needs grammar or an n-gram signal, not a character set. Original report follows. Roughly a quarter to a third of Japanese actor turns contained simplified Chinese: measured 7/30 (23%) by the agent that found it and **9/30 (30%)** on an independent re-probe with a different detector and different samples. It concentrates in the vocab `explanation:` field — the text the learner reads as a study aid — and sometimes reaches the spoken turn itself: `本屋さん → explanation: 书店，专门卖书的地方。`, `4つ星のホテル欢迎您，您好吗。请问我能为您做些什么呢？`, `会議欢迎您，请坐。`. `GREETING_SYS` says "You MUST speak ONLY 100% in {language}" but `validate()` never checks the script, so every one of these scores as a pass — the actor suite's 96–99% is measuring format compliance, not language. **Second, related gap:** `is_closed_question` is ASCII-only by design contract (it matches `?` and `[a-z']+`), so a Japanese turn ending in `？` can never trip it — `列車のチケットが必要ですか。` passed. Half the catalog has the no-closed-question rule entirely unenforced. Both fixes are new assertions in `app/`, not fixture work, and both will move the actor number down before they move it up. |
| OPEN-19 | The vocabulary card rarely reaches the learner mid-conversation | [`app/llm.py`](app/llm.py) (`ACTOR_SYS`), [`scripts/eval_actor.py`](scripts/eval_actor.py) | **Partly fixed 2026-09-10 — cause identified, 0/24 → 6/24, still short of the 21/24 `GREETING_SYS` manages.** The single largest cause is one instruction. Adding `ACTOR_SYS`'s bullets to `GREETING_SYS` one at a time, only `- End every turn with something concrete the learner can grab onto…` moved anything: raw cards 6/8 → **0/8**, while character, C1-speak, lead and C1-structure were all neutral (7/8, 7/8, 6/8, 5/8). **It is the same line `7e312f0` tried to add to `GREETING_SYS` and withdrew for costing vocab cards** — the cost was never mysterious, and the line had been in `ACTOR_SYS` all along. The mechanism is the ending frame: the vocab block follows the dialogue, so an instruction about how the *turn* ends competes with it. Rewordings that keep the frame do not help — "End your SPOKEN DIALOGUE with… the vocabulary block still follows" measured 0/8, "Your last SPOKEN sentence must…" 2/8 — while dropping it entirely ("Give the learner something concrete to grab onto **in your dialogue**") measured 6/8. Applied to `ACTOR_SYS`: **cards 0/24 → 6/24 across three readings, closed-question turns 3/24, `moods` held at 100.0% (96/96, 0 fallbacks), 450 tests green.** **Ruled out, each with numbers:** position (block moved to the very end, zero trailing rules, content byte-identical — 1/24); `task_setup` (0/10 with it and 0/10 without); total size (a diagnostic prompt cut to 2,505 chars, *smaller* than `GREETING_SYS`, still gave 6/24). **What remains is diffuse.** With the reworded rule in place, dropping the topic-policing paragraph, the mood line, the sentence budget or the C1-structure rule each measured 2-3/8 — no second isolatable cause. Closing the gap to `GREETING_SYS` likely needs `ACTOR_SYS` rebuilt on the greeting prompt's skeleton rather than trimmed further, which is a larger change than this. **Production check before/after is the honest next step**: `vocab_log` held 2 rows against 44 sessions. **Note `eval_actor.py:24` still builds all 20 cases from `GREETING_SYS`**, so no gated suite measures this — see OPEN-32.
| OPEN-20 | Conversation history is never trimmed, so the prompt grows without bound | [`app/session.py`](app/session.py) (`recent_history`), [`app/cli.py`](app/cli.py) | **Fixed 2026-09-11.** Only the ACTOR's view is bounded — `recent_history(messages)` hands it the last 20 messages (ten exchanges). The list itself is untouched, because the judge slices it by absolute index (`messages[task_start_idx:]`) and the session log needs it whole; trimming the list would have shifted every index under it. Measured with the real tokenizer over a 69-turn Japanese session: prompt tokens went 902 → 4,234 unbounded (crossing `PROMPT_CACHE_MAX_KV_SIZE` around turn 67) and now hold flat at **1,343 from turn 10 onward**. The scenario, role and task live in the system prompt rather than the history, so the window costs the NPC little — but note it is a guess at the right size, not a measured one: nobody has tested whether ten exchanges is enough context for a late-session turn.
| OPEN-21 | `reactive` is set on ~974 tasks that have no premise for the NPC to establish | [`app/scenarios/data/`](app/scenarios/data/), [`app/llm.py`](app/llm.py) (`build_task_setup_block`) | **Open — suspected, impact not yet measured, 2026-09-07.** `build_task_setup_block` returns "" when a task is neither reactive nor carries a `scene_hint`, and its docstring gives the reason: *"Do not leak the learner's goal to the NPC if the NPC doesn't need to set up anything."* 1,309 catalog tasks are flagged `reactive`; **974 of them carry no `scene_hint`, and only 65 of those name a problem** the NPC could infer from the goal (sold out, wrong item, billing error, and similar). The remainder are ordinary requests — *"Ask where to return rented skis"*, *"Confirm exact bus departure bay number"* — for which the block still fires, handing the NPC the learner's secret objective and a 1,137-character instruction that opens *"HIGHEST PRIORITY THIS TURN, ABOVE VOCABULARY COACHING"*. Two plausible harms, neither yet measured: the NPC pre-empting a goal the learner was meant to reach unprompted, and the vocabulary card being explicitly deprioritized on those turns — see OPEN-19. Worth measuring before any content edit; the flag may simply be over-applied by the generator. |
| OPEN-22 | Learner-facing coach and judge text is never script-checked | [`app/coach.py`](app/coach.py), [`app/judge.py`](app/judge.py), [`app/llm.py`](app/llm.py) (`find_wrong_script`) | **Open — latent, not currently firing, 2026-09-07.** `find_wrong_script` guards the actor's sentences, the vocab block and, since `9e0d1cc`, translated hints. It does **not** guard coach feedback or the judge's reason string, both of which are model-generated Japanese shown to the learner. Probed 14 Japanese coach cases and 8 judge reasons: **0 leaks**, so this is a gap rather than a live bug. It is recorded because every other Japanese-output surface in this project has leaked at some point — the actor at 23-30%, translated hints at 12 of 12 — and the reason each was found late is that the guard existed and simply was not called on that path. |
| OPEN-23 | The apology net corrects correct Japanese, then traps the learner in a no-skip drill | [`app/coach.py`](app/coach.py) (`apply_apology_net`) | **Open — confirmed 2026-09-07. The most severe finding of this audit: it breaks the invariant ARCHITECTURE.md §2 calls load-bearing.** The English marker is subject-anchored — `\b(?:i|we)(?:'m\| am\| will be\|'ll be\| are)\b…\blate\b` — but the Japanese one is `遅れ(?:ま\|そう\|る)\|遅刻\|遅くなり` with no subject constraint, so a learner reporting that the **train** was late is told to apologise for it. Verified directly: `電車が遅れました。` → `❌ "電車が遅れました。" → ✅ "すみません、電車が遅れました。"`, and `バスが遅れそうです。` likewise, while the English twins *"The train is late today."* and *"I got delayed by traffic."* stay clean. The consequence is worse than a bad bullet. `promote_fit` is on in the CLI, so this lands in Feedback: `is_clean_verdict` → `False`, `correction_targets` → `['すみません、電車が遅れました。']`, and `run_correction_drill` ([`app/cli.py:258-267`](app/cli.py)) is a `while True` with no skip — the learner must retype an apology they do not owe, and the only exit is Ctrl-D. The shipped test used `電車が遅れて、会議に間に合いませんでした。` as its stay-silent case, which passes because the learner *was* late there; the single-clause form was never tried. `check_rule_vacuity.py` has no predicate for any of the eight nets, so nothing guards the other seven either. |
| OPEN-24 | A mid-turn MLX error leaks a spinner thread that hangs the process | [`app/cli.py`](app/cli.py) (`:770-780`, `Spinner` at `:31`) | **Open — confirmed 2026-09-07.** The handler stops the name `spinner` (bound at `:565` and `:640`), but the spinners created inside that `try` are `eval_spinner` (`:670`) and `coach_spinner` (`:756`). Neither is stopped, and `threading.Thread(target=self._spin)` is created without `daemon=True`, so the thread keeps writing `\r[Analyzing…]` over the learner's `You:` prompt for the rest of the session **and blocks interpreter exit** — the process does not terminate on `quit`. Verified by driving `cli.main()` with `evaluate_task` raising, real `Spinner`: `threading.enumerate()` shows a live non-daemon thread and the test process had to be killed. Invisible to tests because [`tests/test_cli_session.py:81`](tests/test_cli_session.py) patches `Spinner` with a no-op double. `coach_spinner` was introduced by the turn reorder in `7e312f0`; `eval_spinner` predates it. |
| OPEN-25 | Failed tasks are counted nowhere — the summary and the database both under-report | [`app/cli.py`](app/cli.py) (`:690-697` vs `:627`), [`app/db.py`](app/db.py) | **Open — confirmed 2026-09-07.** The skip branch increments `tasks_skipped`; the max-attempts branch writes a `task_logs` row with `outcome='failed'` and increments nothing, while the summary line is labelled "Tasks Skipped/Failed". One failed task plus one skip reports `Tasks Skipped/Failed: 1` and stores `tasks_skipped=1`. `db.abandon_stale_sessions` and `_finish_resumable` backfill with `SUM(CASE WHEN outcome='skipped')`, so the stored number is consistently wrong and cannot self-heal. `test_task_failed_max_attempts` asserts the log row exists and walks past the counter. |
| OPEN-26 | Translated objectives ship half-transliterated to the learner | [`app/llm.py`](app/llm.py) (`translate_hints`, `:521`) | **Open — confirmed 2026-09-07.** `9e0d1cc` added a guard for the Chinese leak, but the guard is `find_wrong_script`, a simplified-Chinese Han denylist that returns `''` for any Latin text — nothing inspects the translation for untranslated English. Measured over 6 random scenarios and 96 translated goals and hints: **8 contained untranslated English or a broken half-transliteration, and `find_wrong_script` flagged 0 of them.** One is legitimate (`AV技術的な`), so 7 of 96 are defects the learner reads as their objective: `カatering、会場、装飾の財務制限について議論します。`, `コンフィetiの投げ入れに関する政策を確認します。`, `ワheelchair用のプールデッキへのアクセス用スロープ…`, `食料 stall がハラルまたはカシュルの調理認証を持っているか…`. `カatering` and `コンフィeti` are words in no language — the model begins a katakana transliteration and falls back into Latin mid-word. Same learner-visible failure as the Chinese leak, through the half of the hole the fix does not cover. Checked clean alongside it: the `{num}.` prefix parsing (19/19, no fullwidth numbering) and the composed vocab goals correctly bypassing the model. |
| OPEN-27 | `_is_multi_clause` fires on a noun-phrase "and", disabling the judge's false-negative rescue on ~10% of the catalog | [`app/judge.py`](app/judge.py) (`:198-200`, `:293`) | **Open — confirmed 2026-09-07.** `re.search(r'\band\b', done_when)` decides whether `judge_llm` runs the context-free second opinion its own docstring credits with removing every false negative found so far. Measured across all 5,520 tasks: **766 trip the regex, and 601 of those contain no `and <past-tense verb>` at all** — the "and" is inside a noun phrase. `Learner asked about salt and pepper.`, `Learner accepted cheese and jam pairing suggestion.` and `Learner asked about lost-and-found procedures.` all return `True`. So roughly 10% of the catalog silently loses the rescue, against a judge measured at ~24% false negatives on Japanese vocab tasks. `app/judge.py:200` and `:296-300` are both untested. |
| OPEN-28 | The prompt KV cache holds 3 entries for a 4-key working set, so reuse collapses to zero | [`app/llm.py`](app/llm.py) (`PROMPT_CACHE_MAX_ENTRIES`, `:27`) | **Open — confirmed 2026-09-07.** Not the prefix-trim logic, which is correct. The keys in use are `actor`, `coach`, `judge` and `judge_confirm`; capacity is 3. `judge_confirm` enters the rotation on any failed attempt — common, since `MAX_TASK_ATTEMPTS = 4` — producing a cyclic access pattern of 4 against 3 slots. Measured by running the real `_prepare_prompt_cache_for_call` with measured prompt shapes: without `judge_confirm`, reuse is `{actor: 9, coach: 9}`; with it, reuse is `{}` and all four go cold every turn; at capacity 4, reuse returns. It compounds: both judge prompts put the variable goal first and the ~1,100-token rule block last, so their shared prefix is 98-154 characters against a `PROMPT_CACHE_PREFIX_THRESHOLD` of 256 tokens — they can never hit, yet they evict `coach` (7,074 identical characters every turn) and `actor` (3,788 identical within a task). Two slots are permanently held by entries that cannot benefit, on exactly the turns where the learner is struggling. |
| OPEN-29 | `--stats --lang X` reports whichever profile was used last, ignoring the language | [`app/cli.py`](app/cli.py) (`print_stats_report`, `:272-276`) | **Open — confirmed 2026-09-07.** It selects `SELECT id FROM user_profiles ORDER BY last_active DESC, id DESC LIMIT 1` and never filters on language, while every other call site resolves the user through `db.get_or_create_user(conn, target_lang=language)` (`:439`), which keys on `(display_name, target_lang)`. Verified against a copy of the real `~/.language-coach/sessions.db`, which holds an English profile (id 4, 42 sessions) and a Japanese one (id 5, 1 session): both `--stats --lang English` and `--stats --lang Japanese` report 42 sessions and 41 tasks. The Japanese profile's progress is unreachable through the CLI. |
| OPEN-30 | The first task of a session can be reactive, which the guard exists to prevent | [`app/scenarios/models.py`](app/scenarios/models.py) (`:106-116`) | **Open — confirmed 2026-09-07.** The guard looks for the first **phase-2** task and swaps it when reactive, but 13 catalog tasks are `phase=1, reactive=True` and phase-1 tasks sort first, so it inspects the wrong slot. 2,000 simulated `get_session_tasks(num_tasks=10)` calls opened with a reactive task **56 times (2.8%)** — e.g. *"Confirm your emergency contact details"* before *"Greet the agent and state your final destination"*. It also feeds `build_task_setup_block` into the greeting, so the NPC must invent a prior exchange that never happened. Interacts with OPEN-21. |
| OPEN-31 | The vocab-block label regex has diverged between the two actor paths again | [`app/cli.py`](app/cli.py) (`:168`), [`app/llm.py`](app/llm.py) (`:427,540,599,687,852,862`) | **Open — confirmed, currently latent, 2026-09-07.** `cli.py:157-168` documents matching the third label as `encourag\w*` because the model frequently writes `encouragement:`. That fix landed in `cli.py` only; `llm.py` still carries six literal `encourage:` copies across `validate`, `repair_actor_output`, `salvage_actor_output`, `call_actor`'s fallback and both `stream_actor` replay blocks. Measured on real captured output (drift rates 2/24, 2/10, 3/3): at `max_sentences=3`, 4 of 25 real cards are destroyed by `call_actor` — `validate` fails to strip the card, counts it as spoken, returns `Too many sentences (4)`, and `repair_actor_output` then truncates the card away. At `max_sentences=4`, zero. `stream_actor` keeps the card because `_find_vocab_start` needs only `word:`/`explanation:`, so the two paths disagree — the same divergence class as the per-sentence rules fixed in `0df1d3f`, a different rule. Latent only because OPEN-19 means `ACTOR_SYS` emits no card at all today; it goes live the moment OPEN-19 is fixed. |
| OPEN-32 | No eval exercises `stream_actor`, the path the learner actually reads | [`scripts/check_actor_path_parity.py`](scripts/check_actor_path_parity.py), [`scripts/eval_actor.py`](scripts/eval_actor.py) | **Fixed 2026-09-11.** `check_actor_path_parity.py` drives `call_actor`'s assembly and `stream_actor` over the **same bytes** — `stream_actor` takes `generator_fn` precisely so it can be fed captured text — and asserts they agree on whether a vocab card survives, whether the turn is valid, and that neither leaks vocab labels into dialogue. Deterministic and model-free, so it runs in `check_all.sh` and a divergence is caught by the commit that introduces it. Verified non-vacuous against the real bug: reintroducing OPEN-31's literal `encourage:` produces 2 disagreements, on exactly the drifted-label cases. This is the gap that let the per-sentence rules sit diverged for eleven days (`0df1d3f`) and OPEN-31 go unnoticed. **It does not replace a model-based stream measurement** — it cannot see what the model generates, only that the two paths treat it identically — so a streamed-output suite is still worth having if the actor prompt is reworked.
| OPEN-33 | `DEBUG=1` is swallowed by `main.py`, so the documented debug path shows less than no flag at all | [`main.py`](main.py) (`:20-22`), [`README.md`](README.md) (`:76`) | **Open — confirmed 2026-09-07.** `app/cli.py:421,571,772` all `raise` when `DEBUG` is set, so a developer should get a traceback. `main.py` then catches bare `Exception` and prints one line. Forcing a model-load failure: with `DEBUG` unset the developer sees the error, the MLX diagnostic and the setup hint; with `DEBUG=1` they see only `[⚠️ Error during startup: …]` — no traceback and strictly less information. The message also says "during startup" for failures raised mid-session. README documents `DEBUG=1 python3 main.py` as the way to debug. |
| OPEN-34 | Dead and unreachable code that `check_rule_vacuity.py` does not reach | [`app/judge.py`](app/judge.py), [`app/cli.py`](app/cli.py), [`app/db.py`](app/db.py) | **Open — confirmed, cosmetic, 2026-09-07.** `judge.py:124`'s trailing `return None` is unreachable, since `:72` restricts `lang` to four values and both branches return. `judge.py:118-122`, the Japanese `target in user_input` branch, cannot fire from the catalog: 0 of 401 vocab targets contain a Japanese character (they are English by design — see OPEN-18). `cli.py:149-150` tests membership in `NOUN_CAPITALIZING_LANGUAGES` (`de`, `german`, `deutsch`, `luxembourgish`), but `normalize_language` admits only English and Japanese and `main()` exits otherwise, so the intersection is empty. `db.get_scenario_stats` (`:319-345`) has no production caller and carries a byte-identical copy of the mastery ladder in `get_all_scenario_stats` (`:365-372`), free to diverge. |
| OPEN-04 | MLX / Apple Silicon only | [`app/llm.py`](app/llm.py) | **Deliberate scope choice.** Runs directly against MLX on Apple Silicon without a backend abstraction layer (e.g. Ollama, vLLM). |
| OPEN-05 | JSON catalog storage / No authoring UI | [`app/scenarios/data/`](app/scenarios/data/) | **Deliberate scope choice.** Content is stored per-scenario as JSON files; authoring relies on JSON files and maintenance scripts rather than an authoring tool. |
| OPEN-06 | Language support limited to English & Japanese | [`app/cli.py`](app/cli.py) | **Deliberate scope choice.** UI localization, scenario content, and prompt structures support English and Japanese; Thai and other languages are not supported. |

## Recently Resolved

For historical commit logs and detailed change history, see `git log`. Key recent milestones include:

- **Coach punctuation and quote normalization (audit F4/F5, 2026-08-26):** `_normalize_phrase` stripped only ASCII `.!?`, so the guard collapsing a no-op ❌X → ✅X never fired in Japanese — a learner writing 「コーヒーをお願いします」 was told it was wrong and "corrected" to the identical sentence plus 。. The same gap left the "a phrase may appear in Feedback OR Level up, never both" rule unenforced, so the coach corrected *to* とても and then told the learner to change とても. `。．！？` now normalize with their ASCII twins on a parity argument — same three marks, different width. **`、` deliberately excluded:** it is a comma, its ASCII twin `,` is not stripped either, and `❌ "コーヒーを一つ、" → ✅ "コーヒーを一つ"` is a real correction, verified still shown. Separately, `_normalize_quotes` ran on the Level-up block only *after* the promotion loop had read it, so a 「」-quoted correction was never promoted and the learner saw a clean verdict directly above a grammar fix — the BUG-001 shape, still live, and not Japanese-only (curly-quoted English failed identically). Coach headline unmoved at 59.6%; the diff is inert on all 127 captured raws, which also shows **the eval does not cover either bug** — the fixtures are fragments, these shapes need whole-sentence turns, so the unit tests are the only regression protection.

- **OPEN-02 Session resume — done** (`a2c6308`). An interrupted session can now be resumed; `3e0e7c4` followed up on resume noise.
- **OPEN-03 Progress view — done** (`df326e8`). A progress report ships and mastery is surfaced in the chooser, wired to `get_scenario_stats()` in [`app/db.py`](app/db.py).
- **CI and local gate** (`6870b6b`, `a386434`): GitHub Actions workflow plus [`scripts/check_all.sh`](scripts/check_all.sh); a build backend was declared so the editable install works.
- **Judge deterministic matching** (`101439f`): widened from words to phrases and enabled for Japanese.
- **Mood and complication coverage** (`942cb5e`): moods and complications are now exercised and measured.
- **Profile and CLI robustness** (`4a0b642`, `3256088`, `3e0e7c4`): typos no longer fork learner profiles, Ctrl+D/Ctrl+C exit cleanly, and the session-summary crash and 80-line chooser are fixed.
- **Scenario 2 Misalignment Fix:** Replaced 69 job-interview tasks under an airport scenario heading with true airport-relevant tasks.
- **Catalog-Wide Content Polish:** Eliminated near-duplicate goals, trivial vocabulary targets, and goal/`done_when` mismatches across all 5,520 tasks.
- **Actor Output Validation & Repair:** `call_actor` can no longer return text that fails `validate()`. `repair_actor_output` truncates over-length turns and `salvage_actor_output` drops closed yes/no questions, both preserving the vocab block; the diagnostic that used to print at the learner is now behind `DEBUG`.
- **Engine Robustness & Error Handling:** Lazy MLX model loading via `_ensure_model` with the original exception chained, replacing an import-time load whose bare `except` discarded the cause. `MLX_ERRORS` is deliberately **not** narrowed — `mlx-lm` documents no exception contract and [`app/cli.py`](app/cli.py) catches it at top-level boundaries to show a readable message instead of a traceback mid-conversation; the handlers re-raise under `DEBUG` so real bugs still surface.
- **JSON Migration & Codebase Infrastructure:** Migrated scenario data into per-scenario JSON files ([`app/scenarios/data/`](app/scenarios/data/)), simplified [`app/scenarios/builtins.py`](app/scenarios/builtins.py), added [`README.md`](README.md) and [`pyproject.toml`](pyproject.toml), and localized CLI outputs for English and Japanese.
